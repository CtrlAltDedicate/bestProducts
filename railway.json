{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && python3 manage.py migrate && python3 -m gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --workers 1",
    "restartPolicyType": "ON_FAILURE"
  }
}
