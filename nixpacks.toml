[phases.setup]
aptPkgs = ["python3", "python3-pip", "python3-dev"]

[phases.install]
cmds = ["pip3 install --break-system-packages -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 manage.py migrate && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --workers 1"
